tunnel:
  tunnelName: homelab-public
  tunnelId: ${CLOUDFLARED_TUNNEL_ID}
  credentialsSecret: tunnel-credentials
ingress:
  - hostname: "*.${CLUSTER_DOMAIN}"
    service: "http://public.kube-system.svc.cluster.local:80"
  - service: http_status:404
replicaCount: 2
