---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: headlamp-oidc-admin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
  # - kind: User
  #   name: "oidc:you@example.com"   # must match oidc-username-prefix + email claim exactly
  #   apiGroup: rbac.authorization.k8s.io
  - kind: Group
    name: "oidc:infra-admins"   # must match oidc-groups-prefix + whatever group/role comes from Zitadel
    apiGroup: rbac.authorization.k8s.io
