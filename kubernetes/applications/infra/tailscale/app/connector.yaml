---
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: homelab-lan-router
  namespace: infra-tailscale
spec:
  replicas: 1
  hostnamePrefix: k8s-lan-gateway
  subnetRouter:
    advertiseRoutes:
      - "${HOMENET_CIDR}/24"
