# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: immich
  install:
    crds: CreateReplace
    remediation:
      retries: 3
    disableSchemaValidation: true
    skipSchemaValidation: true # Bypass new helm validation due to issue in Flux' helm-controller (https://github.com/helm/helm/issues/32480)
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
    disableSchemaValidation: true
    skipSchemaValidation: true # Bypass new helm validation due to issue in Flux' helm-controller (https://github.com/helm/helm/issues/32480)
  valuesFrom:
    - kind: ConfigMap
      name: immich-values
      valuesKey: values.yaml
