repositories:
  - name: cilium
    url: https://helm.cilium.io

releases:
  - name: cilium
    namespace: kube-system
    chart: cilium/cilium
    version: 1.19.3
    includeCRDs: true        # IMPORTANT for Talos bootstrap
    values:
      - ../../../kubernetes/applications/infra/cilium/values.yaml
