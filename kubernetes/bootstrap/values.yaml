configs:
  params:
    server.insecure: true
applicationSet:
  enabled: true
controller:
  replicas: 1
server:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - argocd.{ MY_DOMAIN }
dex:
  enabled: false
notifications:
  enabled: false
